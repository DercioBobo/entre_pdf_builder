# Playwright is the only dependency not already provided by Frappe.
# WeasyPrint and pypdf/PyPDF2 are Frappe's own pinned dependencies —
# declaring them here would cause version conflicts on bench get-app.
playwright>=1.40.0
